十二动物号码归纳器 v1.1.0 - 最终验收报告
================================================================================

验收日期：2026-07-23
项目路径：C:\Users\2SS2\animal-number-ledger

一、最终结论
--------------------------------------------------------------------------------
状态：通过，可交付 Windows 版本。

本次验收采用隔离数据库和隔离 APPDATA，不读取或修改正式账本。
已验证金额解析、追加、撤销、结算清零、历史结算金额、备份恢复、
数据库完整性、内置自检及 Windows EXE 启动。

二、自动化测试
--------------------------------------------------------------------------------
结果：37 passed，3 subtests passed，0 failed，0 skipped。

覆盖明细：
- 解析器：14 项
- 计算器：5 项
- 数据库：9 项
- 跨日结算：4 项
- 删除功能：3 项
- 备份恢复：2 项

三、隔离端到端业务验收
--------------------------------------------------------------------------------
[通过] 输入 1号0.29、7号1.15、20号2.30，分别保存为 0.29、1.15、2.30。
[通过] 三笔追加后的总账金额为 3.74。
[通过] 输入 1号13.1456 被拒绝，金额最多保留两位小数。
[通过] 撤销最近一次追加后总金额回到 0.00。
[通过] 重新追加并结算后，归档账本固化总账金额 3.74。
[通过] 结算后创建新的活动账本，01-49 金额总和为 0.00。
[通过] 历史账本可读取结算总账金额 3.74。
[通过] JSON 备份通过严格校验并可恢复结算数据。
[通过] 恢复前安全备份成功生成并通过验证。
[通过] SQLite PRAGMA integrity_check 返回 ok。
[通过] 程序内置自检显示 37/37，失败 0，错误 0。

说明：以上为直接调用正式业务模块的自动化端到端验收，不是屏幕坐标模拟。
不同 Windows 显示缩放比例下的字体和控件观感，建议首次使用时目视确认。

四、Windows EXE 验收
--------------------------------------------------------------------------------
文件名：十二动物号码归纳器-v1.1.0.exe
文件大小：13,911,410 字节（约 13.27 MB）
启动结果：通过。程序在隔离 APPDATA 中持续运行，并创建 53,248 字节数据库。

SHA-256：
40c4576e921811999f1669cadc4f691d1cbdfa9a3aad6c5fc63829a32d215460

校验结果：dist\SHA256.txt 与实际 EXE 哈希完全一致。

五、v1.1.0 主要修正
--------------------------------------------------------------------------------
1. 使用 Decimal 精确处理金额，消除 0.29 变 0.28 等浮点截断问题。
2. 拒绝超过两位小数及超出 SQLite INTEGER 范围的金额。
3. 批次、指令和分配在同一事务中原子保存，失败时整体回滚。
4. 结算时固化总账金额，并在主界面和历史记录中显示。
5. 启动时自动结算隔夜活动账本。
6. 旧数据库自动迁移并补算已归档账本的结算金额。
7. 增强备份关系、金额、日期和版本完整性验证。
8. 恢复前自动生成 pre_restore 安全备份。
9. 撤销最近批次按数据库 ID 排序，避免同秒记录顺序错误。

六、数据安全
--------------------------------------------------------------------------------
正式数据库：C:\Users\2SS2\AppData\Roaming\AnimalNumberLedger\ledger.db
验收前记录：53,248 字节；最后修改时间（UTC）2026-07-22 16:17:23。
验收方式：所有功能测试和 EXE 启动均使用工作区内的隔离数据库。
结论：正式数据库未用于本次测试。

七、已知限制
--------------------------------------------------------------------------------
1. Windows 数字签名未完成：当前没有代码签名证书，首次运行可能显示未知发布者。
2. macOS 版本未构建：需要在 macOS 系统上执行构建脚本。
3. 自动化验收不能替代所有显示器缩放比例下的视觉检查。

八、交付清单
--------------------------------------------------------------------------------
[完成] dist\十二动物号码归纳器-v1.1.0.exe
[完成] dist\SHA256.txt
[完成] TEST_REPORT.txt
[完成] FINAL_ACCEPTANCE_v1.1.0.txt
[完成] README.md、QUICKSTART.md、DELIVERY.md、PROJECT_SUMMARY.md

最终状态：Windows v1.1.0 已完成并通过自动化与隔离启动验收。
