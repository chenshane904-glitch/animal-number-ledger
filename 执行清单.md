# 完整推送流程 - 执行清单

## ✅ 检查结果

### GitHub Actions配置：完整 ✓
- ✓ .github/workflows/mac-build.yml 已创建
- ✓ 工作流配置正确
- ✓ 将自动生成 macos-dmg 和 macos-app
- ✓ 所有必需文件就绪

### 预期构建产物：
1. **macos-dmg** (Artifact) - 包含 `十二动物号码归纳器-v1.1.0.dmg`
2. **macos-app** (Artifact) - 包含 `十二动物号码归纳器.app`

---

## 📝 需要执行的步骤

### 步骤1: 安装Git

**Git未安装，需要先安装！**

1. 访问：https://git-scm.com/download/win
2. 点击 "Download for Windows"
3. 下载完成后，双击安装
4. 安装时**使用默认设置**即可
5. 安装完成后**关闭当前PowerShell**
6. **重新打开PowerShell**

验证安装：
```bash
git --version
```
应该显示类似：`git version 2.x.x`

---

### 步骤2: 配置Git（首次使用）

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

**示例**：
```bash
git config --global user.name "Zhang San"
git config --global user.email "zhangsan@example.com"
```

---

### 步骤3: 在GitHub创建仓库

1. 登录GitHub：https://github.com
2. 点击右上角 "+" → "New repository"
3. 填写信息：
   - **Repository name**: `animal-number-ledger`
   - **Description**: 十二动物号码归纳器 - macOS自动构建
   - **可见性**: Public 或 Private（建议Private）
   - **重要**：不要勾选任何选项（README、.gitignore、License）
4. 点击 "Create repository"
5. **复制仓库地址**，例如：
   ```
   https://github.com/你的用户名/animal-number-ledger.git
   ```

---

### 步骤4: 执行Git命令

**复制以下命令，依次执行**：

```bash
# 1. 进入项目目录
cd C:\Users\2SS2\animal-number-ledger

# 2. 初始化Git仓库
git init

# 3. 添加所有文件到暂存区
git add .

# 4. 查看要提交的文件（可选）
git status

# 5. 提交到本地仓库
git commit -m "Setup GitHub Actions for macOS build

- Add GitHub Actions workflow for automatic macOS builds
- Configure PyInstaller to build .app and .dmg
- Add all dependencies to requirements.txt
- Update README with GitHub Actions instructions"

# 6. 连接到远程仓库（替换为你的仓库地址！）
git remote add origin https://github.com/你的用户名/animal-number-ledger.git

# 7. 设置主分支为main
git branch -M main

# 8. 推送到GitHub（首次推送）
git push -u origin main
```

**重要**：第6步的URL要替换成你在步骤3中创建的仓库地址！

---

### 步骤5: 等待自动构建

推送成功后：

1. 访问：`https://github.com/你的用户名/animal-number-ledger/actions`
2. 你会看到 "Setup GitHub Actions for macOS build" 工作流正在运行
3. 状态图标：
   - 🟡 橙色圆圈 = 正在构建
   - ✅ 绿色勾 = 构建成功
   - ❌ 红色叉 = 构建失败
4. 构建时间：约 **5-10分钟**

---

### 步骤6: 下载构建产物

构建成功后：

1. 点击成功的工作流（绿色✓）
2. 滚动到页面底部，找到 "Artifacts" 部分
3. 下载：
   - **macos-dmg** - 下载得到 `macos-dmg.zip`
   - **macos-app** - 下载得到 `macos-app.zip`
4. 解压后即可使用或分发

---

## 🎯 构建产物说明

### macos-dmg.zip
解压后得到：`十二动物号码归纳器-v1.1.0.dmg`

**用途**：Mac安装包（推荐）

**使用方法**：
1. 双击DMG文件
2. 将应用拖到Applications文件夹
3. 右键应用 → 选择"打开"

### macos-app.zip
解压后得到：`十二动物号码归纳器.app`

**用途**：Mac应用程序

**使用方法**：
1. 解压后直接使用
2. 右键应用 → 选择"打开"

---

## ⚠️ 首次打开必须右键

由于应用未签名，Mac会阻止双击打开。

**正确操作**：
1. 右键点击应用
2. 选择"打开"
3. 在弹出警告中点击"打开"
4. **以后可以正常双击打开**

---

## 📱 告诉Mac用户

发送给Mac用户时，请告知：

```
⚠️ 重要提示：

1. 首次打开必须右键 → 打开 → 点击"打开"确认

2. 输入号码时用逗号分隔，不要用句号！
   ✅ 正确: 1,2,3各0.50
   ❌ 错误: 1.2.3各0.50（句号是小数点）
```

---

## 🔄 以后更新代码

修改代码后，只需：

```bash
cd C:\Users\2SS2\animal-number-ledger
git add .
git commit -m "描述你的更改"
git push
```

每次推送都会**自动触发构建**！

---

## 🎛️ 手动触发构建

不推送代码也可以重新构建：

1. 访问：`https://github.com/你的用户名/animal-number-ledger/actions`
2. 点击左侧 "Build macOS App"
3. 点击右上角 "Run workflow"
4. 选择分支（main）
5. 点击绿色 "Run workflow" 按钮

---

## 🐛 常见问题

### Q: Git推送时要求输入用户名密码？
**A**: 
- 输入GitHub用户名
- 密码使用 Personal Access Token（不是账号密码）
- 获取Token：GitHub Settings → Developer settings → Personal access tokens → Generate new token (classic)
- 勾选 `repo` 权限
- 生成后复制Token，推送时粘贴作为密码

### Q: 构建失败了？
**A**: 
1. 点击失败的工作流查看日志
2. 查找红色错误信息
3. 常见原因：
   - requirements.txt格式错误
   - Python代码语法错误
   - 缺少必要文件

### Q: 找不到Artifacts？
**A**: 
- 只有构建成功（绿色✓）才有Artifacts
- 检查构建是否完成
- Artifacts保留30天后自动删除

### Q: DMG在Mac上打不开？
**A**: 
- Mac用户必须右键 → 打开
- 不能双击打开（首次）

---

## 📞 需要帮助？

- 详细指南：`GITHUB_ACTIONS_GUIDE.md`
- 推送步骤：`PUSH_TO_GITHUB.md`
- 项目说明：`README.md`

---

## ✅ 检查清单

推送前确认：

- [ ] Git已安装
- [ ] Git已配置用户信息
- [ ] GitHub仓库已创建
- [ ] 复制了正确的仓库地址
- [ ] 项目目录正确：`C:\Users\2SS2\animal-number-ledger`
- [ ] 所有文件已保存

推送后确认：

- [ ] 推送成功（没有错误提示）
- [ ] GitHub Actions自动开始运行
- [ ] 等待5-10分钟
- [ ] 构建显示绿色✓
- [ ] Artifacts可以下载

---

**当前状态**：所有配置就绪，等待安装Git后推送！
